const fs = require('fs')
const handlebars = require('handlebars')
const chalk = require('chalk') //将日志变为其他颜色
module.exports = async () => {

    /**
     * @description: 编译末班文件
     * @event: 
     * @param {type} filePath 目标文件路径
     * @return {type} templatePath 末班文件路径
     */
    function compile(meta, filePath, templatePath) {

    }
}